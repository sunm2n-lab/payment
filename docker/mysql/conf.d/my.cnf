# S7(격리 수준) / S11(서버 파라미터) 실험에서 쓸 자리다.
# Phase 0 에서는 charset 만 두고 격리 수준·락 관련 파라미터는 건드리지 않는다.
[mysqld]
character-set-server = utf8mb4
collation-server = utf8mb4_0900_ai_ci

[client]
default-character-set = utf8mb4
